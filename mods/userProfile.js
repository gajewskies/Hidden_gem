// seperate file to keep sensitive information from being transfered
var userProfile = function(confiure) {
	this.email = configure.email,
	this.firstname = configure.firstname,
	this.lastname = configure.lastname
};

module.exports = userProfile;
